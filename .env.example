# Resend API key for the contact form serverless function (api/contact.ts).
# Create a free account at https://resend.com, generate a key and set this
# in the Vercel dashboard (Project Settings > Environment Variables) and in
# a local .env file when testing with `vercel dev`.
RESEND_API_KEY=
